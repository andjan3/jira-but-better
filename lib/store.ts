/* import { create } from "zustand";

interface OpenBoard {
  openBoard: number;
  setOpenBoard: (value: number) => void;
}

const useStore = create<OpenBoard>((set) => ({
  openBoard: 0,
  setOpenBoard: (value) => set({ openBoard: value }),
}));

export default useStore; */
